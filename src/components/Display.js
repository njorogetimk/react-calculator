import React from "react";

const Display = ({ display }) => {
  return (
    <div className="display">
      <h5>{display}</h5>
      {/* {console.log(display)} */}
    </div>
  );
};

export default Display;
